@using MudBlazor
@namespace Client.Pages.Components.TabComponents

@* Customer-scoped invoicing tab that uses the shared InvoicingComponent *@
<InvoicingComponent SelectedCustomerId="@CustomerId" SelectedCustomerName="@CustomerName" />

@code {
    [Parameter] public Guid CustomerId { get; set; } = Guid.Empty;
    [Parameter] public string? CustomerName { get; set; }
}